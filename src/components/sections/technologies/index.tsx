import * as React from 'react';
import { Section } from '../../common';

const Technologies = () => {
  return (
    <Section
      variant='background'
      caption='sites with benefits'
      title='Tools chosen to suite all your business needs'
      subTitle='At Nectbox every project is a new story, yet some things never change. We picked a set of tools for each story we might get the oportunity to tell, allowing us to create value for you and your business.'
    />
  );
};

export default Technologies;
